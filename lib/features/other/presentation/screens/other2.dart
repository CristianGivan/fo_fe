import 'package:flutter/material.dart';

class Other2 extends StatelessWidget {
  const Other2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
          child: Text('Other2'),
        ),
      ),
    );
  }
}
