import 'package:flutter/material.dart';

class Other1 extends StatelessWidget {
  const Other1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Other1'),
      ),
    );
  }
}
