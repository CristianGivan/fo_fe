import 'package:flutter/material.dart';

class Other1Screen extends StatelessWidget {
  const Other1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('Other1'),
      ),
    );
  }
}
